<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.tyut.mapper.PersonMapper">
    <cache/>
    <resultMap id="personNestQuery" type="Person">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="age" column="age"/>
        <result property="sex" column="sex"/>
        <association property="card" column="card" javaType="IdCard" select="cn.edu.tyut.mapper.IdCardMapper.getIdCardById"/>
    </resultMap>
    <resultMap id="personNestResult" type="Person">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="age" column="age"/>
        <result property="sex" column="sex"/>
        <association property="card" column="card" javaType="IdCard">
            <id property="id" column="id"/>
            <result property="code" column="code"/>
        </association>
    </resultMap>
    <select id="getPersonById" resultMap="personNestQuery">
        SELECT *
        FROM person
        WHERE id = #{id}
    </select>
    <select id="getPersonById01" resultMap="personNestResult">
        SELECT person.*, idcard.code
        FROM person,
             idcard
        WHERE person.card = idcard.id
          AND person.id = #{id}
    </select>
</mapper>